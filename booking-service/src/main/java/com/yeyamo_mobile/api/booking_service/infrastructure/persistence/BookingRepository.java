package com.yeyamo_mobile.api.booking_service.infrastructure.persistence;import java.time.Instant;import java.util.*;import org.springframework.data.jpa.repository.*;import org.springframework.data.repository.query.Param;import jakarta.persistence.LockModeType;
public interface BookingRepository extends JpaRepository<BookingEntity,UUID>,JpaSpecificationExecutor<BookingEntity>{@Override @EntityGraph(attributePaths="slot")org.springframework.data.domain.Page<BookingEntity>findAll(org.springframework.data.jpa.domain.Specification<BookingEntity>spec,org.springframework.data.domain.Pageable pageable);List<BookingEntity>findByUserIdOrderByCreatedAtDesc(String user);Optional<BookingEntity>findByReference(String reference);@Lock(LockModeType.PESSIMISTIC_WRITE)@Query("select b from BookingEntity b where b.id=:id")Optional<BookingEntity>findLocked(@Param("id")UUID id);
 @Query(value="SELECT COUNT(*),COUNT(*) FILTER (WHERE status='PENDING'),COUNT(*) FILTER (WHERE status='CONFIRMED'),COUNT(*) FILTER (WHERE status='CANCELLED'),COUNT(*) FILTER (WHERE status='COMPLETED'),COALESCE(SUM(total_amount),0) FROM bookings WHERE (:fromAt IS NULL OR created_at>=:fromAt) AND (:toAt IS NULL OR created_at<=:toAt)",nativeQuery=true)Object[] adminStats(@Param("fromAt")Instant fromAt,@Param("toAt")Instant toAt);}
