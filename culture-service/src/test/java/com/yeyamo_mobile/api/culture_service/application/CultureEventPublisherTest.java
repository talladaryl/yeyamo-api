package com.yeyamo_mobile.api.culture_service.application;
import static org.mockito.ArgumentMatchers.any;import static org.mockito.Mockito.verify;import com.fasterxml.jackson.databind.ObjectMapper;import com.yeyamo_mobile.api.culture_service.infrastructure.outbox.*;import java.util.*;import org.junit.jupiter.api.Test;import org.junit.jupiter.api.extension.ExtendWith;import org.mockito.Mock;import org.mockito.junit.jupiter.MockitoExtension;
@ExtendWith(MockitoExtension.class)class CultureEventPublisherTest{@Mock CultureOutboxRepository repository;@Test void writesEventToOutbox(){CultureEventPublisher publisher=new CultureEventPublisher(repository,new ObjectMapper().findAndRegisterModules());UUID id=UUID.randomUUID();publisher.publish("CultureContentCreated",id,Map.of("contentId",id));verify(repository).save(any(CultureOutbox.class));}}
